[pytest]
markers =
    export_acceptance: targeted PDF export acceptance tests for plot sizing, size propagation, and datasheet continuation layout
    qt_slow: Qt-backed Studio tests that are valid but slow enough to exclude from broad quick runs
    gui_workflow: Studio GUI workflow tests that should usually be run by exact node id or small -k chunks
